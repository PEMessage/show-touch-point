# Install

```bash
cd ~/.local/share/gnome-shell/extensions
git clone https://github.com/PEMessage/show-touch-point.git showtouchpoint@example.com
cd showtouchpoint@example.com
glib-compile-schemas schemas
# logout and login to take effect
```

